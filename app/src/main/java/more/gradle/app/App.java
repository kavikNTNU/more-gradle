/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package more.gradle.app;

import more.gradle.list.LinkedList;

import static more.gradle.utilities.StringUtils.join;
import static more.gradle.utilities.StringUtils.split;
import static more.gradle.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
